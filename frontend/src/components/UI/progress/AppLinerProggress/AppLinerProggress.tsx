import LinearProgress from "@mui/material/LinearProgress";
import { styled } from "@mui/material/styles";

const AppLinerProggress = styled(LinearProgress)({
  width: "100%",
  minHeight: "10px",
  borderRadius: "99em",
});

export default AppLinerProggress;
